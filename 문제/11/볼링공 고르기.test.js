function solution1(N, M, array) {
    array.sort()

    const [firstNumber] = array;

    const answer = 0

    // for (let i = 0; i < N - 1; i++) {
    //     for(let ii = 0+; ii<)
    // }


}

describe('', () => {
    it('solution1 실행', () => {
        expect(solution1(5, 3, [1, 3, 2, 3, 2])).toBe(8)
    })
})